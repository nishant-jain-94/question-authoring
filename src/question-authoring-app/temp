static get template() {
          if (!memoizedTemplate) {
            // Retrieve this element's dom-module template
            memoizedTemplate = Polymer.DomModule.import(this.is, 'template');

            // Clone the contents of the superclass template
            let superTemplateContents = document.importNode(QuestionAuthoringApp.template.content, true);

            // Insert the superclass contents
            let fullpage = memoizedTemplate.content.querySelector('#footer');
            //memoizedTemplate.content.insertBefore(superTemplateContents, footer);
            $(document).ready(function() {
          console.log('Hell yeah');
          var free = document.getElementById('fullpage');
          $(free).fullpage({
              navigation: true,
              navigationPosition: 'right',
          });
        });

          }
          return memoizedTemplate;
        }


        // $(document).ready(function() {
        //   console.log('Hell yeah');
        //   var free = document.getElementById('fullpage');
        //   $(free).fullpage({
        //       navigation: true,
        //       navigationPosition: 'right',
        //   });
        // });
